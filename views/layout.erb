<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8">
		<meta name="viewport" content="width=1280px">
		<title>Wine and Dine</title>
		<link rel="stylesheet" type="text/css" href="/stylesheets/styles.css">
		<link href='http://fonts.googleapis.com/css?family=Special+Elite|Voltaire' rel='stylesheet' type='text/css'>
	</head>

	<header class = "navigation">
		<% if @current_user %>
    		<%= erb :'_top_nav_for_users'%>
  		<% else %>
    		<%= erb :'_top_nav_for_nonusers'%>
  		<% end %>
	</header>

	<body>
		<div class = "wrapper">
			<%= yield %>
		</div>
	</body>
</html>
